define(['entities/Vector'], function(Vector) {
    var FlowNode = function(position, direction) {
        this.position = position;
        this.direction = direction;
    };

    FlowNode.prototype = {

    };

    return FlowNode;
});